anthropic>=0.69
faster-whisper>=1.2
yt-dlp>=2026.1
fastapi>=0.115
uvicorn[standard]>=0.34
jinja2>=3.1
python-dotenv>=1.0
python-multipart>=0.0.20
requests>=2.32

# Deteccao de rosto para o reframe 9:16 focado na cena (opencv sem GUI).
# Fixado em 4.x: o 5.0 reestruturou a API e removeu cv2.CascadeClassifier.
opencv-python-headless>=4.10,<5

# DLLs CUDA para o ctranslate2 no Windows (transcricao na GPU)
nvidia-cublas-cu12
nvidia-cudnn-cu12
